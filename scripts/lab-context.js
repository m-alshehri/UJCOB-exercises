import fs from 'node:fs';import vm from 'node:vm';const c={};c.window=c;vm.createContext(c);vm.runInContext(fs.readFileSync('js/lab-content.js','utf8'),c);const out={};for(const [lab,rows] of Object.entries(c.window.LAB_CONTENT)){out[lab]=Object.fromEntries(rows.map(q=>[q.id,{title:q[lab==='web-lab'?0:2],task:q[lab==='web-lab'?1:3]}]));}fs.writeFileSync('lib/lab-context.json',JSON.stringify(out,null,2)+'\n');
